import { Layout } from 'antd';
import { SearchForm, FinalPieChart } from './components';

function App() {
  return (
      <Layout>
        <SearchForm />
        <FinalPieChart />
      </Layout>
  );
}

export default App;
